# React + TypeScript + Vite + Tailwind

daisyUI is a Tailwind plugin, not a standalone library — it generates component classes using Tailwind's engine, so without Tailwind installed, daisyUI produces nothing.
